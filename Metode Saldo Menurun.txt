SELECT
  aset_id,
  nilai_perolehan,
  umur_ekonomis,
  'saldo_menurun' AS metode,
  ROUND(nilai_perolehan * 0.25, 2) AS depresiasi_tahunan
FROM
  `project.dataset.aset_tetap`
WHERE
  metode = 'saldo_menurun';
